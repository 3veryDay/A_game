spring.application.name=SecurityDemo

#dev purpose
spring.security.user.name = demo1
spring.security.user.password = 0000

# delete h2

# set to valid .
spring.app.jwtSecret = mySecretKey123!
spring.app.jwtExpirationMs = 86400000

# Postgresql database
spring.datasource.url = jdbc:postgresql://localhost:5432/<db>
spring.datasource.username=postgres
spring.datasource.password = <password>

spring.jpa.hibernate.ddl-auto = update
#this is the dialect, 
spring.jpa.database-platgorm = org.hibernate.dialect.PostgreSQLDialect
